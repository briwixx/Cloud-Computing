## 💡 Using the same Azure Resource Group

To ensure all team members deploy into the exact same Azure Resource Group (already created by Terraform), follow these steps:

### 1. Login to Azure
```powershell
az login
az account set --subscription <SUBSCRIPTION_ID>
